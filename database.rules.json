{
  "rules": {
    ".read": false,
    ".write": false,

    "products": {
      ".read": true,
      ".write": "auth != null && auth.token.email === 'admin@galamerch.com'"
    },

    "alumniTicketBundles": {
      ".read": true,
      ".write": "auth != null && auth.token.email === 'admin@galamerch.com'"
    },

    "orders": {
      ".indexOn": ["userId", "userEmail"],

      ".read": "auth != null && (auth.token.email === 'admin@galamerch.com' || (query.orderByChild === 'userId' && query.equalTo === auth.uid) || (query.orderByChild === 'userEmail' && query.equalTo === auth.token.email))",

      ".write": "(!data.exists() && (auth == null || (auth != null && newData.child('userId').val() === auth.uid) || (auth != null && newData.child('userId').val() !== null && newData.child('userId').val().beginsWith('guest-')))) || (auth != null && auth.token.email === 'admin@galamerch.com')",

      ".validate": "newData.hasChildren(['id', 'items', 'totalPrice', 'status'])"
    },

    "alumniTickets": {
      ".indexOn": ["userId", "userEmail", "orderId"],

      ".read": "auth != null && (auth.token.email === 'admin@galamerch.com' || (query.orderByChild === 'userId' && query.equalTo === auth.uid) || (query.orderByChild === 'userEmail' && query.equalTo === auth.token.email))",

      ".write": "(!data.exists() && (auth == null || (auth != null && newData.child('userId').val() === auth.uid) || (auth != null && newData.child('userId').val() !== null && newData.child('userId').val().beginsWith('guest-')))) || (auth != null && auth.token.email === 'admin@galamerch.com')"
    },

    "siteSettings": {
      ".read": true,
      ".write": "auth != null && auth.token.email === 'admin@galamerch.com'"
    },

    "gallery": {
      ".read": true,
      ".write": "auth != null && auth.token.email === 'admin@galamerch.com'"
    }
  }
}
